The complete network is described in Architekturvorschlag.pdf. The loss function is already implemented in the
base class.

- V02
  - Komplexeres Embedding (wird nur konfiguriert)
  - Embedding-Output tanh (wird nur konfiguriert)
  - Zusätzliche Ausgabe von Embeddings