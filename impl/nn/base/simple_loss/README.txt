The used loss function is described in "Architekturvorschlag.pdf". The network itself is not used in this class, only
the defined loss function.

- V02: Zusätzliche Loss-Funktionen / Regularisierungen wie in Task 26 definiert können genutzt werden
  - V02 is almost compatible to V01. It has some minor changes, but it includes all features. From now V01 is obsolete.
